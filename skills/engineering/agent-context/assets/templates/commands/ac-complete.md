---
description: Complete and archive the active agent-context task
---
Perform the Complete action from the agent-context skill (`references/procedures.md`): after proportional verification, set the active task to `completed` and move it to `docs/context/archive/`, then refresh the generated files.
