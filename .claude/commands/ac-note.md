---
description: Add an item to the agent-context backlog
argument-hint: <thing you want to do next>
---
Append the following to `docs/context/backlog.md` as a new unchecked backlog item, following the Backlog section of the agent-context skill (`references/procedures.md`). Do not start work on it now — just capture it:

$ARGUMENTS
